<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="WIM Details"
    Width="850"
    Height="550"
    Background="#F8F9FA"
    FontFamily="Segoe UI"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock
            x:Name="HeaderText"
            Grid.Row="0"
            Margin="0,0,0,15"
            FontSize="16"
            FontWeight="SemiBold"
            Foreground="#2B579A" />

        <!-- Content -->
        <Border Grid.Row="1" Background="White" CornerRadius="4" Padding="0" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.08" />
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="#2B579A" CornerRadius="4,4,0,0" Padding="12,8">
                    <TextBlock Text="Image Information" FontWeight="SemiBold" Foreground="White" FontSize="13" />
                </Border>

                <ListView
                    x:Name="WimDetailsList"
                    Grid.Row="1"
                    BorderThickness="0"
                    SelectionMode="Extended">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="160" DisplayMemberBinding="{Binding Name}" Header="Name" />
                            <GridViewColumn Width="220" DisplayMemberBinding="{Binding Path}" Header="Path" />
                            <GridViewColumn Width="60" DisplayMemberBinding="{Binding Index}" Header="Index" />
                            <GridViewColumn Width="90" DisplayMemberBinding="{Binding Architecture}" Header="Architecture" />
                            <GridViewColumn Width="100" DisplayMemberBinding="{Binding Version}" Header="Version" />
                            <GridViewColumn Width="80" DisplayMemberBinding="{Binding SizeGB}" Header="Size (GB)" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                x:Name="StatusText"
                Grid.Column="0"
                VerticalAlignment="Center"
                Foreground="#586069"
                FontSize="12" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="CopyDetailsButton" Margin="0,0,8,0" Content="Copy Details" />
                <Button x:Name="RefreshButton" Margin="0,0,8,0" Content="Refresh" Background="#6A737D" />
                <Button x:Name="CloseButton" Content="Close" Background="#6A737D" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
